<div id="newsfeed">
    <h1>News feed</h1>

    {{# each articles }}
        <div class="article">
            <h2>{{ this.title }}</h2>
            <img src="{{ this.imgSrc }}">
            <p>{{this.ingress}}</p>
        </div>
    {{/each}}
</div>
